<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.daum.dao.GuestMapper">
  
  <!-- 해당 SQL을 메서드와 연결해서 처리. id는 메서드명 resultType은 return
  		parameterType은 매개변수 -->
  <select id="getAllList" resultType="net.daum.vo.GuestVO">
    SELECT *
    FROM tbl_guest25
  </select>

</mapper>